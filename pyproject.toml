[project]
name = "2026-03-09-students-app"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.9"
dependencies = ["flask", "pymysql", "pytest", "cryptography", "requests"]
